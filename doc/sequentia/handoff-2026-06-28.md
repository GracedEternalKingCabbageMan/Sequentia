# Handoff — 2026-06-28: order-book DEX (#6) + anchor consensus

Self-contained resume notes (no prior-session context assumed). Two active workstreams:
the cross-chain **order-book DEX** (the main directive) and the **anchor reorg-of-reorg**
consensus fix (just deployed).

## TL;DR — where things stand

- **Consensus (done + deploying):** the anchor reorg-of-reorg recovery fix is committed
  (`23b1be0b1`, branch `claude/sequentia-bitcoin-sidechain-w6xady` of `SequentiaByClaude`),
  reviewed twice, and was rolling-deployed to the live committee on 2026-06-28. THREE changes
  shipped; a FOURTH (the committee-equivocation finality-split tie-break) is **left for Alberto**
  — see the PDF `alberto-anchor-changes-2026-06-28.pdf` and
  `doc/sequentia/anchor-reorg-of-reorg-recovery-design.md`.
- **Order book #6 (resume here):** Phase 1 (same-chain book) and Phase 2a (cross-chain courier
  protocol) are done. **Phase 2b onward is the next work.** Repo `~/seqdex`, branch `main`,
  HEAD `f37be4a`.
- **Swaps:** the interim cross-chain RFQ (`seqdexd`) now works BOTH directions on testnet
  (reverse sell GOLD→BTC, forward buy OILX with BTC, both confirmed by the user this session).
  The order book is meant to REPLACE this RFQ; the RFQ is the fallback until it does.

---

## A. Consensus: anchor reorg-of-reorg recovery (SHIPPED)

Repo `SequentiaByClaude`, commit `23b1be0b1`. Design + rationale:
`doc/sequentia/anchor-reorg-of-reorg-recovery-design.md`. Test:
`test/functional/feature_pos_reorg_of_reorg_recovery.py` (passes; asserts recovery across a
restart restores the original blocks verbatim, AND that a manual `invalidateblock` is NOT
resurrected across a restart).

The three shipped changes (all node-local, NO consensus-rule change, so old/new binaries are
consensus-compatible):
1. **Seed the recovery worklist at startup** (`SeedAnchorInvalidated` ← `LoadBlockIndex`) so the
   anchor watcher's recovery survives a restart.
2. **Dedicated provenance marker** `BLOCK_FAILED_ANCHOR = 512` (chain.h, OUTSIDE
   `BLOCK_FAILED_MASK`) set ONLY by the watcher (`CChainState::MarkAnchorInvalid`), cleared in
   `ResetBlockFailureFlags`. Distinguishes anchor invalidations from `invalidateblock`/consensus
   failures so those stay durably invalid across restart (the first adversarial review caught
   that inferring provenance from `BLOCK_FAILED_VALID` alone was a deploy-blocker).
3. **Negative anchor cache** (`g_anchor_stale_cache`, cleared on parent tip change) so the
   every-tick reconsider loop RPC-checks each orphaned anchor at most once per parent-tip epoch
   (no self-inflicted bitcoind RPC storm); plus break-on-NO_CONNECTION and a once-per-batch
   `RecalculateBestHeader`.

Files touched: `src/chain.h`, `src/anchor.{h,cpp}`, `src/validation.{h,cpp}`,
`src/node/blockstorage.cpp`.

**Change 4 — NEEDS ALBERTO (not implemented).** Committee equivocation: two different blocks at
the SAME height both anchored to the SAME (valid) Bitcoin block reach quorum among disjoint
committee subsets; the minority then can't reorg to the longer chain (immediate finality), and
anchoring can't break the tie (both anchors OK). Operational band-aid = `invalidateblock` the
stuck minority tip (preserved across restart by the marker above). Real fix = a consensus
tie-break rule (yield finality to a same-anchor, strictly-better-certified sibling). Open
questions are in the design doc and the Alberto PDF. NOTE: the live 96/4 split this session
resolved itself when a deep testnet4 reorg rolled the chain back below the split — but the gap
remains.

### Deploy status / how it was deployed
- Laptop binary is **depends-static** → scp'd to the box (binaries bypass git by design). Built
  at `build-linux/src/elementsd`, md5 `3511de420f57a65e893441ca67bedcea`.
- Detached deploy script `/root/reorgfix-deploy.sh` on the box (logs `/root/reorgfix-deploy.log`):
  canary-gates node099 (hash-match vs node000) then runs the proven `/root/rolling-anchorfix.sh`
  (committee in batches of 6 + gateway + explorer-node) and reloads node-gw's `treasury-clean`.
- **dexnode was handled separately** (it runs from `/root/sequentia/SequentiaByClaude/src/elementsd`,
  RPC 18300; reload wallets `seqdex-mm-btc` + `w` after restart). VERIFY on resume that dexnode is
  on the new binary (`md5sum`) and its wallets are loaded.
- Rollback: live binaries backed up as `elementsd.bak-prereorgfix` (old md5 `2e08208c…`) at both
  `/root/SequentiaByClaude/src/` and `/root/sequentia/SequentiaByClaude/src/`; mv back + restart.
- **On resume, confirm the deploy finished cleanly:** `tail /root/reorgfix-deploy.log` should end
  with "committee+gateway+explorer done"; committee-up = 100; committee md5 = `3511de42…`; chain
  producing with `anchorstatus:"ok"`.

---

## B. Order-book DEX (#6) — THE MAIN DIRECTIVE (resume here)

Goal: turn the web-wallet Swap tab into a real P2P **order-book** DEX (not the RFQ model), for
BOTH same-chain atomic swaps AND cross-chain BTC↔asset HTLC swaps. The RFQ
(`seqdexd`) was only ever to bootstrap a market-maker; the order book is the real product.

Repo: **`~/seqdex`** (Go, branch **`main`** — work goes directly on main, NOT claude/ branches).
Design docs (in `SequentiaByClaude/doc/sequentia/`): `seqob-orderbook-design.md`,
`cross-chain-orderbook-consolidation.md`.

Architecture:
- **Relay** `cmd/seqobd` — settlement-AGNOSTIC: couriers OPAQUE E2E-sealed swap messages by
  session_id and serves resting SIGNED offers; never parses settlement. E2E `Crypter` = ECDH
  secp256k1 → AES-256-GCM (`internal/seqob/client/crypter.go`).
- **Maker** `cmd/seqob-maker` (our MM; anyone can make the same way). `-mode samechain|cross`.
- **Taker** `cmd/seqob-cli`.
- Proto: `daemon/api-spec/protobuf/seqob/v1/{offer,relay}.proto`. Offer `Settlement` oneof
  `{SameChainTerms=20, CrossChainTerms=21, LightningTerms=22}`.

### Done
- **Phase 1** (same-chain order book): live on the box under systemd (`seqobd` + `seqob-maker`,
  binaries in `/root/seqob-test/`, Restart=always) — see memory `seqob-box-runtime.md`.
- **Phase 2a** (`667216a` + `f37be4a`): cross-chain offers can be POSTED + VALIDATED in the book
  (`CrossChainTerms`, `offer/sentinel.go` BTC sentinel + directions, `validator/validator.go`
  `checkCrossChain`, `seqob-maker -mode cross` `buildCrossOffer`), and the cross-chain LIFT
  COURIER PROTOCOL is defined: `internal/seqob/client/xcourier.go` (`XcMsg` tagged union, `XcLeg`,
  the FORWARD + REVERSE handshake message sequences in its header comment, `Seal`/`OpenXcMsg`).

### NEXT — Phase 2b onward (NOT started)
1. **2b — forward driver.** Implement `RunTakerForward` / `RunMakerForward` (new
   `internal/seqob/client/xdriver.go`, mirroring same-chain `driver.go`) executing the FORWARD
   courier handshake from `xcourier.go` (taker pays BTC, secret holder = TAKER) over the proven
   `pkg/xchain` HTLC settlement. pkg/xchain API (already proven by the RFQ): `NewSwapBitcoin(btc,
   seq, prim)`; taker = `LockBTCLeg` → `VerifySeqLegSafe` (anchor gate) → `ClaimSEQLeg` (reveals
   s); maker = `VerifyBTCLeg` → `LockSEQLeg` → `WatchSEQClaim` → `InjectSecret` → `ClaimBTCLeg`.
2. **2c — wire the maker serve loop.** `cmd/seqob-maker/main.go` `serve()` currently calls the
   one-shot same-chain `maker.HandleRequest` (line ~183). Add a multi-round cross-chain path that
   drives `RunMakerForward` per session (keyed by session_id like the same-chain crypters map),
   wire the `pkg/xchain` BTC backend (real testnet4 bitcoind) + SEQ backend (dexnode), and add a
   cross-chain taker to `cmd/seqob-cli`.
3. **2d — prove forward on testnet** via the CLI taker against the maker, end to end.
4. **2e — reverse direction** (ASSET→BTC; secret holder = MAKER; maker `LockBTCLeg` first), per
   the REVERSE sequence in `xcourier.go`.
5. **2f — refunds + restart recovery** (CLTV refunds both legs; resume in-flight lifts).
6. **Phase 3 — wallet codec + courier lift:** `~/sequentia-web-wallet` posts SIGNED offers and
   lifts over the courier (replace the RFQ `/v1/xchain/*` HTTP calls in `xswap.js`/`xrswap.js`).
7. **Phase 4 — rewire the Swap tab to PREFER the book:** show the `seqobd` book; let a user START
   a market by posting their own offer (wallet as maker); NEVER error "no price" (see memory
   `seqob-wallet-orderbook-ux.md` — the current `swap.js` still hits the dead RFQ
   `/v1/market/price`). 0-conf policy per `sequentia-def-0conf-finality-policy` (anchoring surfaced
   honestly, never "final" at 0-conf).
8. **Phase 5 — retire the RFQ** (`seqdexd /v1/xchain/*`) once the book covers both directions.

Reuse the existing MM BTC reserve `seqdex-mm-btc` (box testnet4 node 18300/48332); don't re-fund.

---

## C. Cross-chain RFQ (`seqdexd`) — the interim the order book replaces

`seqdexd` (TDEX fork) = `cmd/tdexd`, systemd `seqdexd.service`, binary `/root/seqdex-bin/seqdexd`,
env `/root/seqdex-data/daemon.env` (`SEQDEX_XCHAIN_WALLET=seqdex-mm-btc`, SEQ RPC 18300, parent
testnet4 RPC 48332). Endpoints (POST): forward `/v1/xchain/{markets,quote,propose,swap}`, reverse
`/v1/xchain/reverse/{quote,open,submit}`. In-memory swap state (wiped on restart).

This session's RFQ fixes (all committed on `~/seqdex` main): `3031979` best-effort `Mine(1)` on
live testnet; `121f2dc` size the SEQ-leg fee by the open-fee-market rate (fix `-25 maxfeerate`);
`029f8dc` configurable quote TTL default 60m (fix forward `QUOTE_EXPIRED`); `8aa6184` `LockSEQLeg`
waits for the SEQ funding tx to confirm (fix forward `SEQ_LOCK_FAILED`). Wallet fix `c2d46476`
(`~/sequentia-web-wallet`): guard the Propose button against double-click (the cross-chain quote
is single-use; a re-click hit "unknown or expired quote_id").

Known interim gotcha: the maker's per-asset SEQ reserve in `seqdex-mm-btc` can be drained by
repeated forward attempts (HTLC fundings) and a reorg can leave them unconfirmed → "reserve 0".
Top up that wallet's asset from `w` if needed (testnet).

---

## D. Operational / deploy notes

- **Box** `159.195.15.140`, SSH `~/.ssh/seq_tunnel` (root). Committee node000–099 run
  `/root/SequentiaByClaude/src/elementsd` (in-tree build, **NO .git** — deploy by scp'ing the
  laptop binary, NOT git pull). dexnode/explorer run `/root/sequentia/SequentiaByClaude/src/elementsd`.
  Node datadirs `/root/seq-testnet/node0NN`, chain subdir `testnet3`. Restart a node with just
  `elementsd -datadir=<dd> -daemon` (per-node config lives in each datadir); `/root/seq-committee-start.sh`
  relaunches all stopped nodes; `/root/rolling-anchorfix.sh` is the proven batched rolling restart.
- **⚠ SSH/conntrack gotcha (bit me this session):** the box drops `ct state invalid` packets, and
  flaky-wifi timeouts + AGGRESSIVE retry loops create stale conntrack entries that get my fresh
  SYNs dropped (port 22 times out while port 80 stays up — looks like a ban, ISN'T one; fail2ban
  is not installed). Be GENTLE: use SSH **ControlMaster** multiplexing (one persistent connection
  reused by all commands), single spaced attempts, NO tight loops, run long ops DETACHED + poll.
  Recovery if locked out: ask the user to run `sudo conntrack -F` on the box console (my IP can't
  fix it — it's the affected IP). See memory `sequentia-server-deploy.md`.
- Deploy pipeline: build/test on laptop (`pop-os`) → commit (author Andreas Kohl) → push via gh
  `aejkcs50` to the right repo (node/consensus = `SequentiaByClaude`; order book = `seqdex` on
  `main`; web wallet = `sequentia-web-wallet`) → binaries scp'd to the box.

---

## E. Immediate next actions on resume

1. **Verify the consensus deploy landed clean** (Section A: deploy log ends OK, committee md5
   `3511de42…`, dexnode on new binary + wallets loaded, chain producing + anchor ok). Clean up
   `/root/reorgfix-deploy.log` + `.bak-prereorgfix` once confirmed healthy.
2. **Get Alberto's answer on Change 4** (the finality-split tie-break) before implementing it.
3. **Resume order book at Phase 2b** (Section B): write `xdriver.go` `RunTakerForward`/
   `RunMakerForward`, wire the maker serve loop + cli taker, prove forward on testnet, then reverse.
4. Keep the RFQ running as the fallback until the book covers both directions (Phase 5 retires it).
