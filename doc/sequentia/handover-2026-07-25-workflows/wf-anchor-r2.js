export const meta = {
  name: 'anchor-precondition-r2',
  description: 'Close the fund-loss holes the verify found in the anchor precondition, and replace the flat timeout with a protocol-deadline-bounded wait per the owner ruling',
  phases: [ { title: 'Fix' }, { title: 'Verify' } ],
}

const SEQDEX = '/home/aejkohl/seqdex'
const WEB = '/home/aejkohl/sequentia-web-wallet'

const RULES = `SEQUENTIA FIRST PRINCIPLE: Bitcoin anchoring is supreme consensus law; Sequentia reorgs whenever Bitcoin reorgs. NEVER weaken an anchor check.

THE SAFETY PROPERTY (established, do not touch): the claimant gate at ${SEQDEX}/daemon/pkg/xchain/orchestrator.go:209 is "anchor >= btcLegHeight AND anchorstatus == ok AND (!certPresent || certified)", read off the block that CONFIRMED THE ASSET-LEG FUNDING. It protects the ASSET-LEG FUNDER (forward: the maker; reverse: the taker) against this attack: if the asset block anchors BELOW the BTC-leg height, a small Bitcoin reorg orphans the BTC leg, the taker double-spends its BTC HTLC input, but the asset block survives (its anchor is below the fork point) so the asset claim stands. The funder loses the asset and gets nothing. Requiring anchor >= H_btc makes "BTC leg reverts" imply "asset leg reverts". NEVER relax it, never add a bypass, and never accept a BURYING block as evidence (invalidation propagates to descendants, never ancestors).

THE FIX ALREADY IN THE WORKING TREE (uncommitted, keep it): the asset FUNDER waits until its own node anchor view reaches the BTC-leg height BEFORE broadcasting (waitSeqAnchorReachesBTCLeg, xdriver.go, called forward at xdriver.go:948 and reverse at xdriver_reverse.go:277), plus ErrAnchorOrderingTerminal so a doomed claimant refunds promptly instead of spinning on an immutable value. Anchors are monotonic (consensus R2), so once the tip qualifies every later block does.

OWNER RULING (Andreas, binding, 2026-07-25): "we should let users decide if the wait is intolerable and they want to cancel the trade (putting the makers order back to rest), rather than cancel it automatically. We cannot really predict how long contested blocks will take to clear anyway." Therefore: NO arbitrary wall-clock timeout is allowed to abort the wait. The ONLY automatic stop permitted is the PROTOCOL DEADLINE, i.e. the point past which funding would be unsafe because the timelock claim window has run down. The current patch raised AnchorWait 20m to 45m and SeqLockWait 15m to 60m; those flat numbers are exactly what the ruling rejects and must be replaced by deadline-derived bounds. (User-initiated cancellation and the wallet UX for it are a SEPARATE follow-up round; do not build the UI here, but do make the wait loop accept a cancellation signal, e.g. a context.Context, so that round can wire it without another refactor.)`

const HOLES = `HOLES THE ADVERSARIAL VERIFY FOUND IN THE CURRENT PATCH. Fix every one.

1. [FUND-LOSS] No BTC-leg RE-VERIFICATION after the wait. ${SEQDEX}/daemon/internal/seqob/client/xdriver.go:914 verifies the BTC leg, :939 then waits up to 45m for the anchor, :948 funds with NO re-check. In that window the taker can double-spend the BTC HTLC input (a 1-block testnet4 reorg suffices) and the maker funds its asset leg against a dead BTC leg. Mirror sites: xdriver_reverse.go:251,277,285; ${WEB}/xmaker.js:305-322,332; ${WEB}/xrswap.js:835,852,858. FIX: re-run the full VerifyBTCLeg (same txid, vout, amount, script, minConf) immediately AFTER the anchor wait and immediately BEFORE LockSEQLeg, aborting if the leg vanished or changed height.

2. [DEAD RAIL - DELETE, DO NOT HARDEN] The verify flagged a stranded-funds path on the RFQ rail (${SEQDEX}/daemon/internal/core/application/xchainmaker/watcher.go:153 and ${SEQDEX}/daemon/cmd/seqdex-xchain-reverse-taker/main.go:224). RFQ IS RETIRED - the owner confirmed it, and it is verified dead in practice: seqdexd still listens on :9945 but there is NO Caddy route to it and the only connection is the daemon to itself. So DO NOT spend safety work hardening it. Instead REMOVE the dead client paths so nothing can route onto a rail that cannot complete:\n   - ${WEB}/xswap.js: the legacy RFQ quote path (dexPost /v1/xchain/quote around :438, /v1/xchain/markets around :280) and any composer FALLBACK that calls X.quote. In ${WEB}/swap.js requoteCross the XBOOK/X.quote fallback must GO: the unified book plus the WS courier is the only real path, and deleting the fallback also deletes the whole-offer OVERSHOOT branch (q.overshoot / q.wholeOffer and the "more than you entered" wording) that violates the every-offer-is-partially-fillable rule. When the unified book is genuinely unreachable, show the plain could-not-load-the-order-book message and disable Place - never route to a dead rail and never overshoot.\n   - ${WEB}/xrswap.js: the reverse RFQ paths (dexPost /v1/xchain/reverse/quote, /reverse/open, /reverse/submit, /v1/xchain/swap). Keep the WS courier reverse flow.\n   - Leave the Go RFQ code in place but do NOT modify it; just confirm in your report which Go files are now unreachable from any live entry point so they can be deleted in a separate cleanup.\n   Removing dead code must not break the courier paths: run the full test suites and say explicitly what still routes through the courier.

3. [SAFETY-WEAKENED] T_seq headroom is not re-checked, so the maker can fund with almost no claim window left. seqLocktime is frozen at terms time as seqTip+240 (about 2h at 30s slots, xdriver.go:824), but the driver can now burn SeqLockWait then AnchorWait before funding at :948. FIX (this is also how the owner ruling gets implemented): derive the wait deadline FROM THE TIMELOCK, not from a flat constant. Before entering the wait, compute the latest safe funding moment from the remaining T_seq window (and T_btc where it binds), clamp the wait to it, re-read the SEQ tip immediately before LockSEQLeg, and abort if seqTip + MinSeqClaimWindow >= seqLocktime. Remove the flat AnchorWait/SeqLockWait abort semantics for this precondition (they may remain as ordinary RPC/step timeouts elsewhere, but must not be what cancels a healthy wait).

4. [CORRECTNESS] Off-by-one between funder and claimant. The funder waits until anchorTip == verifiedBtc.Height and funds the INSTANT equality holds (btc_backend.go:324 computes BlockCount-confs+1); the claimant derives hp from two non-atomic RPCs (xdriver.go:531-533) so a Bitcoin block landing between them yields a higher H and the gate fails. FIX: the funder wait target is safety-monotone (waiting longer is never unsafe), so give it slack: target = max(verifiedBtc.Height, courier-reported funded.Leg.Height) + 1; and derive the leg height atomically where possible.

5. [SPEC-VIOLATION] The reverse asset-funder lacks the forward assertion. xdriver.go:987-1004 now asserts the confirming block committed anchor >= BTC-leg height before announcing; xdriver_reverse.go:316-319 still does "anchorH, aerr := SeqAnchorHeightOf(...); if aerr != nil { anchorH = 0 }" and announces anyway. ${WEB}/xrswap.js has no equivalent at all. FIX: apply the same assertion before sending XcSeqLegFunded (poll a few times; on read failure or anchorH < BTC-leg height send XcFail("seq_leg_underanchored")), and mirror it in xrswap.js.

6. [CORRECTNESS] The claimant gate caches the confirming block hash. xdriver.go:618 (also xdriver_reverse.go:714 and :875) captures verified.BlockHash once; getblockheader answers for ORPHANED blocks too, so a Sequentia reorg that re-mines the leg into a better-anchored block is invisible, and an orphan freezes the verdict. FIX: re-derive the confirming block hash with SeqBlockHashOfTx(leg.Funded.TxID) at the top of every gate iteration and require the block to be on the active chain (getblockheader confirmations > 0) before trusting its anchor.

7. [CORRECTNESS] JS null-anchor is treated as terminal. ${WEB}/xswap.js:1242 with ${WEB}/tooling/lsp/lsp-server.mjs:2815 returning anchor_height: null while the leg tx is unknown to the node: Number(null) === 0 is finite, so fetchLegAnchorEvidence yields anchor 0, verifyAnchor(0) skips the unconfirmed branch and returns unsafe, and awaitAnchor gives up immediately. FIX: treat a null or absent anchor_height as "not yet known" (for example map it to -1) so it takes the unconfirmed WAIT path, never the terminal-unsafe path.

8. [SPEC-VIOLATION, comment only] xdriver.go:993 and ${WEB}/xmaker.js:352 claim that withholding XcSeqLegLocked prevents the taker from claiming; it does not (the taker mints the secret and holds MakerRefundPub, so it can rebuild the redeem script and find the P2SH on chain). FIX: downgrade that comment to courtesy-only and state that the pre-funding precondition is the actual defence. Do not add a fake guarantee.`

const IMPL = { type:'object', additionalProperties:false, required:['fixed','builds','build_output','fund_safety_notes','residual'], properties:{
  fixed:{type:'array',items:{type:'object',additionalProperties:false,required:['hole','how'],properties:{hole:{type:'string'},how:{type:'string'}}}},
  builds:{type:'boolean'}, build_output:{type:'string'}, fund_safety_notes:{type:'string'}, residual:{type:'string'} } }

phase('Fix')
const impl = await agent(`${RULES}\n\n${HOLES}\n\nFix ALL EIGHT holes in ${SEQDEX} (Go; daemon is its own module: export PATH=$HOME/dev-tools/go/bin:$PATH && cd ${SEQDEX}/daemon && go build ./... && go vet ./cmd/... ./internal/... && go test ./internal/seqob/client/ ./pkg/xchain/ ./cmd/... -count=1) and in ${WEB} (node --check the touched files && node --test *.test.mjs tooling/lsp/*.test.mjs).\nKeep the existing uncommitted anchor-precondition work; you are hardening it, not replacing it. Do NOT weaken the claimant gate. Add regression tests that FAIL without each fix, especially: the maker refuses to fund when the BTC leg was double-spent during the wait; the reverse taker refunds instead of stranding; and the wait aborts at the timelock deadline rather than at a flat wall-clock constant.\nNOTE pkg/trade tests fail PRE-EXISTING (needs a live explorer on localhost:3001) - ignore that package, do not fix it, but say so.\nReport the REAL build/vet/test output.`, { label:'fix:r2', phase:'Fix', schema: IMPL })

phase('Verify')
const VERD = { type:'object', additionalProperties:false, required:['lens','pass','holes'], properties:{ lens:{type:'string'}, pass:{type:'boolean'},
  holes:{type:'array',items:{type:'object',additionalProperties:false,required:['severity','where','scenario','fix'],properties:{ severity:{type:'string',enum:['fund-loss','safety-weakened','correctness','spec-violation']}, where:{type:'string'}, scenario:{type:'string',description:'under 400 chars'}, fix:{type:'string'} }}} } }

const verdicts = await parallel([
  { key:'no-new-fund-loss', focus:'the window between verifying the BTC leg and funding the asset leg can no longer be exploited (the BTC leg is re-verified immediately before funding on EVERY asset-funder path, including the RFQ watcher and the reverse taker), and no path can strand funds without a refund branch; enumerate every asset-funding call site across seqdex and the web wallet and show each is covered' },
  { key:'deadline-not-timer', focus:'no healthy anchor wait is aborted by an arbitrary wall-clock constant anywhere (owner ruling); the only automatic stop is the timelock/protocol deadline, the T_seq claim window is re-checked immediately before funding, and the wait loop accepts a cancellation signal so user-initiated cancel can be wired later without a refactor' },
  { key:'gate-intact-and-consistent', focus:'the claimant gate is still byte-identical in effect and never accepts a burying block; funder and claimant agree on the BTC-leg height (no off-by-one), the confirming block hash is re-derived and checked to be on the active chain, forward and reverse enforce the SAME assertions, Go and JS enforce the same rule, and a null anchor waits rather than failing terminally' },
].map(l => () => agent(`ADVERSARIAL review of the uncommitted round-2 anchor work in ${SEQDEX} and ${WEB}. ${RULES}\n\nTry HARD to REFUTE: ${l.focus}. Trace real code, construct a concrete exploit or stuck-funds sequence, cite file:line. Under 400 chars per hole; empty array only if genuinely clean. Default pass=false on ANY fund-loss, stranded-funds path, weakened gate, or arbitrary-timer abort of a healthy wait. READ-ONLY.`,
  { label:`verify:${l.key}`, phase:'Verify', schema: VERD }).then(v=>({ ...v, key:l.key })).catch(()=>({ key:l.key, pass:false, holes:[{severity:'correctness',where:'agent-error',scenario:'no return',fix:'re-run'}] }))))

return { impl, verdicts }
