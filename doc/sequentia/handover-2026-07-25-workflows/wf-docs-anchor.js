export const meta = {
  name: 'document-exprace-and-anchor-backoff',
  description: 'Document the exponential-race leader-election hard fork, and the contested-parent anchor back-off caveat, keeping one-Sequentia-block tracking as the ideal and typical case',
  phases: [ { title: 'Write' }, { title: 'Verify' } ],
}

const NODE = '/home/aejkohl/SequentiaByClaude'

const FACTS = `VERIFIED TECHNICAL FACTS (from a code investigation today; cite/keep accurate, do not restate loosely).

EXPONENTIAL-RACE LEADER ELECTION (hard fork, currently UNDOCUMENTED in doc/ even though three code comments and a help string point readers at doc/sequentia/04-proof-of-stake.md):
- Commit d507fa5c5; release 23.3.7 (500c945a7). Height-gated by PosExpRaceActive; the parameter is pos_exprace_height, settable via -posexpraceheight (src/chainparamsbase.cpp:65). Activated on the current testnet at height 44300. Because it changes WHICH block wins, it is a coordinated HARD FORK.
- New score: -ln(beta / 2^256) * total_weight / weight  (PosVrfScoreExp / PosVrfSlotExp, src/pos.cpp:427-500). Legacy score was U * total_weight / weight (PosVrfSlot), i.e. the raw VRF output scaled by stake.
- The property it buys: P(staker i produces the winning candidate) = weight_i / total_weight EXACTLY, and splitting one stake across several identities leaves that probability UNCHANGED. Under the legacy rule, splitting a stake slightly improved the odds. So the fork makes election exactly stake-proportional and split-proof (Sybil-neutral). This is about STAKE SPLITTING, NOT about chain splits or reorgs.
- Cadence unchanged: P(the leader takes slot 0) = 1 - (1 - 1/n)^n legacy versus 1 - e^-1 exp-race; both about 0.632 at n=20. Slot interval still 30 seconds.
- Candidate RANKING is unchanged in its primary key: freshest anchor first (src/pos_producer.cpp:972-978, "if (a.cand->block->m_anchor_height != b.cand->block->m_anchor_height) return ... > ...; // fresher anchor first"). The fork only replaced the SECONDARY tiebreak (lowest raw VRF beta) with the lowest exp-race score, among candidates carrying the IDENTICAL anchor height. The fork touches NO line of src/anchor.cpp or src/anchor.h.

CONTESTED-PARENT ANCHOR BACK-OFF ("Fix A"), commit 5a032a454 (2026-07-13), nine days BEFORE the exp-race fork:
- Producer-side POLICY, not consensus: -anchoravoidcontested, DEFAULT_ANCHOR_AVOID_CONTESTED = true (src/anchor.h:38). Its own comment: "do not advance the anchor onto a parent-chain height a competing branch is currently contesting. Back the target down to the last block common to all live rival branches, so a new Sequentia block anchors to Bitcoin ground every current contender agrees on and needs no Sequentia reorg when the parent fork resolves." (src/anchor.cpp:325-342)
- Mechanics: baseline target is the parent tip adjusted by -anchorminconf (soft-set to 1 on testnet, src/init.cpp:837, so the baseline IS the live tip). AnchorUncontestedHeight (src/anchor.cpp:304-316) then lowers it to min(rival_tip_height - branchlen) across every non-active, non-invalid getchaintips branch whose tip is within -anchorcontestwindow (DEFAULT_ANCHOR_CONTEST_WINDOW = 2, src/anchor.h:43) of the active tip. A monotonic clamp (src/anchor.cpp:343) means a deeper back-off never lowers an existing anchor; it FREEZES at the previous anchor until the contest clears, then jumps forward.
- Motivation, from the commit message: reduce the finalized-history unwind seen in the 2026-07-11/12 testnet4 anchor-split incidents. See also doc/sequentia/incident-2026-07-17-finality-partition.md.
- Consensus is UNAFFECTED. Anchor validity is exactly three rules (src/validation.cpp:4747-4802): R1 anchor present; R2 anchor height monotonically non-decreasing along the chain, with equal height forcing equal hash; R3 the anchor is on the parent chain best chain at the claimed height (skipped when unchanged from the parent). Anchoring to the naked parent tip remains fully valid. Repeating the previous anchor is valid indefinitely. There is NO minimum-depth rule and NO maximum-advance rule.
- Consequence to state honestly: while the parent chain is contested, the anchor can trail the parent tip by more than one Sequentia block, with no bound in the rules. Measured on testnet4 (about 10,000 Sequentia blocks): trail median 3 parent blocks, p99 15 to 23, maximum observed 26. Testnet4 is pathological here (it carried over 2,000 competing tips during the measurement, and every committee node in this testbed queries a single bitcoind, so all producers compute the identical back-off).

EDITORIAL DIRECTION FROM THE PROJECT OWNER (Andreas), today: keep "the tip tracks Bitcoin within one Sequentia block" as the IDEAL and TYPICAL case. It remains generally true on mainnet, which sees far fewer contested blocks than testnet4. Document the contested-parent back-off as the EXCEPTION and its practical consequence, do NOT rewrite the ideal as if it were wrong.

HOUSE WRITING RULES (non-negotiable):
- NO em dashes anywhere. Use a comma, a period, a colon, or a spaced hyphen.
- The network and protocol is "Sequentia". NEVER abbreviate it as "SEQ". Never write "the SEQ chain"; write "the Sequentia network" or "the Sequentia chain".
- SEQ is the TICKER of the native token, whose NAME is "Sequence" (tSEQ on testnet). Introduce it as "the Sequence token (SEQ)" if it comes up.
- Write final, publishable prose in one pass. No "for now", no "v1", no TODO placeholders.
- Match the voice and structure of the surrounding doc/sequentia files.`

const WRITE_SCHEMA = { type:'object', additionalProperties:false, required:['files_changed','summary','residual'], properties:{
  files_changed:{type:'array',items:{type:'object',additionalProperties:false,required:['path','what'],properties:{path:{type:'string'},what:{type:'string'}}}},
  summary:{type:'string'}, residual:{type:'string'} } }

phase('Write')
const written = await agent(`${FACTS}\n\nRepo ${NODE} (branch pos-exprace). Read doc/sequentia/03-bitcoin-anchoring.md and doc/sequentia/04-proof-of-stake.md FIRST and match their existing voice, heading style and depth. Then:\n\n1. DOCUMENT THE EXPONENTIAL-RACE LEADER ELECTION in doc/sequentia/04-proof-of-stake.md (the code comments and the -posexpraceheight help string already point readers here, so this is the expected home). Cover: what the old sortition was, what the new score is, the exact property it buys (stake-proportional and split-proof, with the plain-language explanation of why splitting a stake no longer helps), that cadence is unchanged, that it is a height-gated coordinated hard fork with the parameter name, and that candidate ranking still prefers the freshest anchor with the exp-race score only breaking ties at equal anchor height. Be precise about the maths but explain it so a reader who is not a cryptographer understands WHY it matters.\n\n2. DOCUMENT THE CONTESTED-PARENT ANCHOR BACK-OFF in doc/sequentia/03-bitcoin-anchoring.md, near the existing passage that says the tip tracks Bitcoin within one Sequentia block (around lines 170-172). KEEP that statement as the ideal and typical case per the owner direction. Add the exception: when the parent chain has live competing branches at or near the tip, the producer holds the anchor at the last height every contender agrees on, so a parent-chain fork resolving does not force a Sequentia reorg. State that this is producer policy and not a consensus rule, name the flags (-anchoravoidcontested, -anchorcontestwindow, -anchorminconf), state that consensus validity is only the three rules, and state the practical consequence honestly: while a contest is live the anchor can trail by more than one block, and applications that require a Sequentia block anchored at or above a specific parent height (notably purely on-chain cross-chain swaps) may wait longer, rarely on mainnet, routinely on testnet4. Give the measured testnet4 figures and say why testnet4 is pathological.\n\n3. If doc/sequentia/04-proof-of-stake.md contains a similar unqualified anchor-freshness claim (around lines 432-434), add a one-line cross-reference to the anchoring doc rather than duplicating the explanation.\n\nEdit the real files. Do not create new top-level docs unless a file genuinely does not exist. Report exactly what you changed.`, { label:'write:docs', phase:'Write', schema: WRITE_SCHEMA })

phase('Verify')
const VERD = { type:'object', additionalProperties:false, required:['lens','pass','issues'], properties:{ lens:{type:'string'}, pass:{type:'boolean'},
  issues:{type:'array',items:{type:'object',additionalProperties:false,required:['severity','where','problem','fix'],properties:{ severity:{type:'string',enum:['inaccuracy','style-violation','omission','contradiction']}, where:{type:'string'}, problem:{type:'string',description:'under 300 chars'}, fix:{type:'string'} }}} } }

const verdicts = await parallel([
  { key:'technical-accuracy', focus:'every technical claim in the new documentation matches the actual code: the exp-race score formula and its stake-proportional split-proof property, the height gating and hard-fork nature, that cadence is unchanged, that freshest-anchor-first is still the primary ranking key and exp-race only breaks ties at equal anchor height, that the anchor back-off is producer policy and not consensus, the three consensus validity rules, the flag names and defaults, and the measured trail figures. Verify against src/pos.cpp, src/pos_producer.cpp, src/anchor.cpp, src/anchor.h, src/validation.cpp, src/init.cpp, src/chainparamsbase.cpp' },
  { key:'style-and-principles', focus:'NO em dashes anywhere in the new text; "Sequentia" is never abbreviated as "SEQ"; the native token if mentioned is named Sequence with ticker SEQ; the ideal case (tip tracks Bitcoin within one Sequentia block) is PRESERVED as ideal and typical rather than rewritten as wrong; nothing contradicts the first principle that Bitcoin anchoring is supreme consensus law and Sequentia reorgs whenever Bitcoin reorgs; the prose is final and publishable with no placeholders, and matches the surrounding docs voice' },
].map(l => () => agent(`${FACTS}\n\nReview the uncommitted documentation changes in ${NODE}/doc/sequentia/ (git diff). Try HARD to find problems with: ${l.focus}. Read the actual source files to check every claim. Report each issue with the exact location and a concrete correction, under 300 chars each. Empty array only if genuinely clean. Default pass=false on ANY inaccuracy or any em dash. READ-ONLY.`,
  { label:`verify:${l.key}`, phase:'Verify', schema: VERD }).then(v=>({ ...v, key:l.key })).catch(()=>({ key:l.key, pass:false, issues:[{severity:'omission',where:'agent-error',problem:'no return',fix:'re-run'}] }))))

return { written, verdicts }
