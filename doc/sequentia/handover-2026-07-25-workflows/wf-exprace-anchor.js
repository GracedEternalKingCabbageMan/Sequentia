export const meta = {
  name: 'exprace-anchor-selection',
  description: 'Determine exactly how the exp-race split-neutral sortition hard fork changed Sequentia anchor selection, whether the anchor deliberately trails the Bitcoin tip, and what that means for the cross-chain swap ordering rule',
  phases: [ { title: 'Investigate' }, { title: 'Synthesize' } ],
}

const NODE = '/home/aejkohl/SequentiaByClaude'
const SEQDEX = '/home/aejkohl/seqdex'

const CONTEXT = `SEQUENTIA FIRST PRINCIPLE (supreme, never contradict): Bitcoin anchoring is supreme consensus law. Every Sequentia block references a Bitcoin block header; Sequentia reorgs whenever Bitcoin reorgs, in real time. Precedence: Bitcoin anchoring > checkpoints > immediate finality. Real-time reorg-following is what makes cross-chain atomic swaps safe. NEVER propose disabling anchor validation.

THE QUESTION. Andreas (project owner) says the observed behavior is about CONTESTED CHAINS: "we recently changed consensus slightly to avoid those in order to have fewer reorgs". The recent change is the exponential-race leader election (split-neutral sortition) behind the pos_exprace_height hard fork, activated at Sequentia height 44300. Node repo ${NODE}, branch pos-exprace; relevant commits: d507fa5c5 "pos: exponential-race leader election (split-neutral sortition) behind pos_exprace_height hard fork" and 500c945a7 "build: bump to 23.3.7 for the exponential-race leader-election hard fork". Historically (per the formal PoS spec at ${NODE}/doc/sequentia/proof-of-stake.html and .pdf) the committee ranked candidate blocks FRESHEST-ANCHOR-FIRST then lowest-VRF.

OBSERVED (live testnet4, real cross-chain swap, Sequentia height ~47.8k-48k, i.e. AFTER the fork activated): a taker BTC HTLC was included in Bitcoin block 145609. The maker waited for that inclusion (1 confirmation), THEN broadcast the Sequentia leg. The Sequentia block that included it referenced Bitcoin block 145607 (two BEHIND a Bitcoin block that already existed), with anchorstatus="ok" and quorum-certified=true. A later Sequentia block referenced 145623. The swap taker refuses to claim unless the Sequentia leg anchor height >= the BTC leg height, so it aborted safely (nothing lost, BTC refundable).

HYPOTHESIS TO TEST: split-neutral sortition makes producers anchor to a Bitcoin block the whole committee has certainly seen (a settled/common anchor), rather than racing to the freshest header, because racing on the freshest header creates contested chains and reorgs. If so, the anchor STRUCTURALLY trails the Bitcoin tip by some amount, by design, and the cross-chain ordering rule must account for it.`

const SCHEMA = { type:'object', additionalProperties:false, required:['lens','findings','conclusion'], properties:{
  lens:{type:'string'},
  findings:{type:'array',items:{type:'object',additionalProperties:false,required:['claim','evidence'],properties:{claim:{type:'string'},evidence:{type:'string',description:'file:line, git diff hunk, or command output'}}}},
  conclusion:{type:'string'} } }

phase('Investigate')
const inv = await parallel([
  () => agent(`${CONTEXT}\n\nLENS 1 - WHAT THE HARD FORK ACTUALLY CHANGED. In ${NODE} (branch pos-exprace) read the exp-race commits (git show d507fa5c5, git show 500c945a7, and git log around them) and the current code. Determine PRECISELY:\n(a) What does exponential-race / split-neutral sortition change about LEADER ELECTION and about BLOCK RANKING? Quote the diff hunks.\n(b) Did it change ANCHOR SELECTION - how a block producer chooses which Bitcoin block header to reference - or the ranking that used to be freshest-anchor-first? If freshest-anchor-first was removed, weakened, or replaced, quote exactly what replaced it and why (read the commit message and code comments).\n(c) Is there now any rule that the anchor must be a Bitcoin block of some minimum depth, or must match/lag the parent block anchor, or must be one the committee has quorum-certified? Find the validation code that decides whether a candidate anchor is acceptable (grep for anchor, anchorheight, anchorstatus, mainchain, contested, split, sortition).\n(d) Was this change specifically motivated by CONTESTED CHAINS / reorgs? Quote the commit message and any design note.\nAlso check ${NODE}/doc/sequentia/*.md for Alberto notes on this (the alberto-*.md files are chronological, later ones SUPERSEDE earlier ones).\nCite exact file:line and diff hunks. READ-ONLY.`, { label:'lens:fork-diff', phase:'Investigate', schema: SCHEMA }),

  () => agent(`${CONTEXT}\n\nLENS 2 - ANCHOR SELECTION MECHANICS IN CODE. In ${NODE} find and explain the code path by which a block producer picks the Bitcoin header to anchor a new Sequentia block to, and the code by which validators ACCEPT or REJECT that anchor.\nAnswer:\n(a) Where does the producer read the Bitcoin tip from (the mainchain RPC / tunnel), and does it use the tip, or tip minus K, or the last anchor it can prove the committee saw? file:line.\n(b) What are the VALIDITY constraints on a new block anchor relative to the parent block anchor (monotonic? may repeat? max advance? min depth?) file:line.\n(c) What does anchorstatus="ok" and quorum-certified=true actually assert? file:line.\n(d) Given (a)-(c), is a systematic 1-2 block trail behind the Bitcoin tip EXPECTED BY DESIGN, or a symptom of a lagging mainchain feed? Answer definitively from the code, and state what the maximum by-design trail is.\nCite file:line. READ-ONLY.`, { label:'lens:anchor-mechanics', phase:'Investigate', schema: SCHEMA }),

  () => agent(`${CONTEXT}\n\nLENS 3 - MEASURE IT LIVE (ssh seq). Characterize the anchor-vs-Bitcoin-tip relationship empirically; do not theorize.\n(a) Sample over at least 4-5 minutes (Sequentia blocks are ~30s): Bitcoin testnet4 tip height (bitcoin-cli -testnet4 getblockcount) alongside the anchorheight of the newest Sequentia block (Sequentia node at 127.0.0.1:18300, rpcuser seq rpcpassword seq, via ${NODE}/src/elements-cli or elements-cli: getbestblockhash then getblockheader, looking for anchor fields). Record a time series.\n(b) Is the offset CONSTANT (suggesting a by-design rule, e.g. always tip-1 or tip-2) or VARIABLE/spiky (suggesting a feed race or lag)? Report the distribution.\n(c) After a NEW Bitcoin block appears, how many Sequentia blocks and how many seconds until a Sequentia block references it or higher? Measure directly - this is the number that decides how long a cross-chain swap must wait.\n(d) Do DIFFERENT committee nodes report the same anchor at the same time (check a couple of the 20 nodes under /root/seq-testnet/node0XX), or do they diverge (which would itself be the contested-chain problem)?\n(e) Check whether the exp-race fork is active on the box: current Sequentia height vs the pos_exprace_height activation (44300), and the running binary version (should be 23.3.7).\nReport concrete numbers. Read-only RPCs only.`, { label:'lens:measure-live', phase:'Investigate', schema: SCHEMA }),
])

phase('Synthesize')
const OUT = { type:'object', additionalProperties:false, required:['by_design','explanation','max_trail','swap_implication','recommended_fix','weakens_safety','needs_user_decision','open_questions'], properties:{
  by_design:{type:'boolean', description:'true if the anchor trailing the Bitcoin tip is intended consensus behavior after the exp-race fork'},
  explanation:{type:'string', description:'plain-language: what split-neutral sortition does and why the anchor trails'},
  max_trail:{type:'string', description:'the by-design maximum trail, and the measured typical time for the anchor to pass a given Bitcoin height'},
  swap_implication:{type:'string', description:'what this means for the cross-chain ordering rule that the SEQ leg anchor must be >= the BTC leg height'},
  recommended_fix:{type:'string'},
  weakens_safety:{type:'boolean'},
  needs_user_decision:{type:'boolean'},
  open_questions:{type:'array',items:{type:'string'}} } }

const synth = await agent(`${CONTEXT}\n\nThree investigation reports:\n\nFORK DIFF: ${JSON.stringify(inv[0])}\n\nANCHOR MECHANICS: ${JSON.stringify(inv[1])}\n\nLIVE MEASUREMENT: ${JSON.stringify(inv[2])}\n\nSynthesize a definitive answer for the project owner.\n1. Is the anchor trailing the Bitcoin tip BY DESIGN after the exp-race split-neutral sortition fork (Andreas hypothesis), or is it a lagging mainchain feed? Decide from the evidence; if the evidence is genuinely inconclusive, say so rather than guessing.\n2. Explain in plain language what split-neutral sortition does about contested chains and why it produces (or does not produce) the trail.\n3. State the by-design maximum trail and the MEASURED time for the anchor to advance past a given Bitcoin height. That number is the real cost imposed on every cross-chain swap.\n4. Implication for the cross-chain ordering rule (taker refuses to claim unless SEQ leg anchor >= BTC leg height, ${SEQDEX} xchain/xdriver): if the anchor structurally trails, the rule can never be satisfied at the instant the BTC leg confirms, so the swap must WAIT for the anchor to advance. Is the correct fix (i) the taker gate polling until a BURYING Sequentia block anchors at/above the BTC leg height (waiting, not weakening), (ii) the maker delaying its SEQ lock until the chain anchor passes the BTC leg height, or (iii) restating the rule? Which preserves the exact reorg-safety property, and what latency does each impose?\n5. Flag anything that would WEAKEN the anchoring safety property - never recommend that; set weakens_safety and needs_user_decision.\nBe precise and cite the evidence. This goes straight to the project owner, who knows this system far better than we do, so state uncertainty honestly rather than papering over it.`, { label:'synthesize', phase:'Synthesize', schema: OUT })

return { inv, synth }
